# Öffentliches Projekt: immer die öffentliche npm-Registry verwenden,
# damit das Lockfile keine firmeninternen Artifactory-URLs enthält
# (Vercel kann artifactory.six-group.net nicht erreichen).
registry=https://registry.npmjs.org/
